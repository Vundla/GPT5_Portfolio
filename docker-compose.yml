version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: vundla
      POSTGRES_PASSWORD: Mv@20217$
      POSTGRES_DB: GPT5_DB
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
